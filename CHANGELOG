# v0.3.4
1. Error handlers now dont fail when callback is invalid

# v0.3.4
1. Error handlers now can be used at the end of the sequence

# v0.3.3
1. Fixed a critical bug with sequences never ending

# v0.3.2
1. Running an empty sequence now does not throw an error

# v0.3.1
1. Perfomance upgrade

# v0.3.0
1. Added voidArguments

# v0.2.0
1. Added callOnError

# v0.1.3
1. Fixed `aseq.append` thinking second argument is invalid, when `typeof == function`

# v0.1.2
1. Fixed aseq not being downloaded properly from npm

# v0.1.1
1. Made aseq not pass callback to the last function in the sequence (caused bugs)

# v0.1.0
1. Initial release
